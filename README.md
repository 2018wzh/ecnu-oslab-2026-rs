# ECNU OSLab 2026 · Rust

本课程通过一系列实验，逐步实现一个简单的操作系统。本仓库使用 Rust 语言，支持 QEMU virt 和 VisionFive2。

`master` 是课程入口，实验指导书和代码位于 `lab-1` 到 `lab-9` 分支。第一次实验从 `lab-1` 开始：

```bash
git switch -c lab-1 origin/lab-1
```

每次实验在上一次实验的基础上继续发展。请保留自己的实现，通过 rebase 获取下一阶段的教师框架，并及时提交工作。具体操作见各实验 README。
